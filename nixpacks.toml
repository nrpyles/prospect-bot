# Fallback for Railway if it falls back to Nixpacks instead of Railpack.
providers = ['node']

[phases.setup]
nixPkgs = ['nodejs_22', 'pnpm']

[phases.install]
cmds = ['pnpm install --frozen-lockfile']

[phases.build]
cmds = ['pnpm build']

[start]
cmd = 'pnpm start'
